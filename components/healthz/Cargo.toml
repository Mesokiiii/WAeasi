[package]
name        = "healthz"
description = "WAeasi component — readiness/liveness probe over WASI HTTP"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true

[lib]
crate-type = ["cdylib"]

[target.'cfg(target_arch = "wasm32")'.dependencies]
