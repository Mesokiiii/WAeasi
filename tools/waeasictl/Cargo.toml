[package]
name        = "waeasictl"
description = "WAeasi host-side CLI — list / run / logs / metrics"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true

[[bin]]
name = "waeasictl"
path = "src/main.rs"
